const express = require('express');
// Import and require mysql2
const mysql = require('mysql2');




// delete employee
dp.delete(`/api/delete/:id`, (req, res) => {
    const val = req.params.id;
    db.query ()
})